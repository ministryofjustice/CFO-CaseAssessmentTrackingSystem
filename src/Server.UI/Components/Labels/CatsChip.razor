<MudTooltip Text="@Description">
    @if (Icon.AsMudIcon() is string icon)
    {
        <MudChip 
            T="string"        
            Variant="Variant.AsMudVariant()"       
            Color="Colour.AsMudColour()"        
            Size="Size.Small" 
            Icon="@icon"
            OnClose="OnClose"
            Label="true"
            Disabled="Disabled">
            @Label
        </MudChip>
    }
    else
    {
        <MudChip 
            T="string" 
            Variant="Variant.AsMudVariant()" 
            Color="Colour.AsMudColour()" 
            Size="Size.Small"
            OnClose="OnClose"
            Label="true"
            Disabled="Disabled">
            @Label
        </MudChip>
    }
</MudTooltip>