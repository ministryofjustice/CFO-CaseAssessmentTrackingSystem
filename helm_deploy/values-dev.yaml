# Development overrides. Namespace: cfocats-dev

# The IRSA-annotated service account (name == namespace) used by all workloads and hooks.
serviceAccountName: cfocats-dev

app:
  serviceAccountName: cfocats-dev
  replicaCount: 3
  ingress:
    host: cfocats-dev.live.cloud-platform.service.justice.gov.uk
  env:
    DOTNET_ENVIRONMENT: "Development"
    Sentry__Environment: "Development-CloudPlatform"

worker:
  serviceAccountName: cfocats-dev
  replicaCount: 1
  env:
    DOTNET_ENVIRONMENT: "Development"
    Sentry__Environment: "Development-CloudPlatform"

# RDS bridge - disabled in non-development environments
rdsPortForward:
  enabled: true

# todo: enable prometheus alerts
# generic-prometheus-alerts:
#   alertSeverity: cfo-alerts-nonprod
