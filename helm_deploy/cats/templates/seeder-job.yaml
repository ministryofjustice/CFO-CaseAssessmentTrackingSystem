{{- if eq .Values.job "seed" }}
apiVersion: batch/v1
kind: Job
metadata:
  # Revision-suffixed so each upgrade of the cats-seed release runs a fresh Job
  # (a Job's pod template is immutable, so a stable name could not be re-applied).
  name: cats-seeder-{{ .Release.Revision }}
  labels:
    app: seeder
spec:
  backoffLimit: {{ .Values.seeder.backoffLimit }}
  # Auto-clean finished Jobs; Helm also prunes the previous revision's Job on upgrade.
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        app: seeder
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      restartPolicy: Never
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        runAsUser: 1001
        runAsGroup: 1001
        runAsNonRoot: true
      containers:
        - name: seeder
          image: "{{ .Values.seeder.image.repository }}:{{ .Values.seeder.image.tag }}"
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            capabilities:
              drop:
                - ALL
          env:
            {{- include "cats.databaseEnv" . | nindent 12 }}
{{- end }}
